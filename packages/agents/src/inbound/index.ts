// Client Inbound Agent — handles inbound conversations for client businesses.
// Uses business knowledge + policy to answer, qualify, route, or escalate.
// Runtime implementation is out of scope for the foundation phase.

export type InboundAgentConfig = {
  organizationId: string;
};
