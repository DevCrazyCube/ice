# Application
NODE_ENV=development
LOG_LEVEL=info

# API
PORT=3001

# Database (Postgres — not yet wired)
DATABASE_URL=postgresql://user:password@localhost:5432/ice

# Queue (Redis — not yet wired)
REDIS_URL=redis://localhost:6379

# Security
# JWT_SECRET=changeme
# WEBHOOK_SECRET=changeme
