// Acquisition Agent — handles new lead conversations.
// Qualifies interest, answers basic product questions,
// moves leads toward a next step (booking, signup, handoff).
// Runtime implementation is out of scope for the foundation phase.

export type AcquisitionAgentConfig = {
  organisationId: string;
};
